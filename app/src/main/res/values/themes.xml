<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskifyToolkit" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TaskifyToolkit" parent="Base.Theme.TaskifyToolkit">
        <item name="materialAlertDialogTheme">@style/App.AlertDialogTheme</item>
    </style>

    <style name="App.AlertDialogTheme" parent="ThemeOverlay.Material3.Dialog.Alert">
        <item name="colorSurfaceContainer">@color/white</item>

        <item name="materialAlertDialogTitleTextStyle">@style/App.AlertDialog.TitleText</item>
        <item name="materialAlertDialogBodyTextStyle">@style/App.AlertDialog.BodyText</item>

        <item name="buttonBarPositiveButtonStyle">@style/App.AlertDialog.Button</item>
        <item name="buttonBarNegativeButtonStyle">@style/App.AlertDialog.Button</item>
    </style>

    <style name="App.AlertDialog.TitleText" parent="MaterialAlertDialog.Material3.Title.Text">
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">22sp</item>
    </style>

    <style name="App.AlertDialog.BodyText" parent="MaterialAlertDialog.Material3.Body.Text">
        <item name="android:textColor">#555555</item> <item name="android:textSize">16sp</item>
    </style>

    <style name="App.AlertDialog.Button" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:textColor">@color/primary_purple</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CustomTabTextStyle" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.TaskifyToolkit.DialogActivity" parent="Theme.TaskifyToolkit">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>